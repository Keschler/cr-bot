Blindly localize the single own troop or building event in {PACKAGE_FILE} for
{RUN_DIR}. Write only {OUTPUT_FILE}. Use only the package and attached pixels.
Do not read source, docs, tests, ground truth, labels, caches, predictions,
detector output, reports, other decisions, or transcripts.

The fixed card, elixir cost, and approximate event frame identify the play.
Find the matching white card name and purple cost badge in the clean timeline,
but do not treat that floating UI badge as the placement or add a fixed offset.
Compare pre-onset frames against every later frame and isolate the first new
own blue deployment clock, spawn glow, body, group, or building belonging to
that release. Exclude actors already present before the overlay and exclude the
new actor's later movement.

Read the initial footprint center from the exact 18x32 grid sheets as
`[column,row]`. The grid may be zoomed to rows 14 through 31; those are the
original row numbers. For a multi-body troop such as Skeletons use the centroid
of the group's initial footprint, not one member. For a building use its placed
footprint center. Use `spawn_center` or `deployment_center` as permitted, cite
a listed supporting frame, and copy both artifact lists exactly.

Return `direct` only if the initial center is visible. If tower/combat occlusion
forces inference, return `inferred` and explain the release anchor, first blue
clock/glow, and uncertainty.

Output:
{{
  "run_id": "<package run_id>",
  "stage": "own_localization_chunk",
  "target_range": "<exact package target_range>",
  "annotation_session_id": "{SESSION_ID}",
  "model": "{MODEL}",
  "reasoning_effort": "{REASONING_EFFORT}",
  "decisions": [{{
    "event_id": "<exact target event_id>",
    "location_frame_index": {EXAMPLE_FRAME},
    "location_rule": "<one allowed target rule>",
    "cell": [0, 0],
    "macro_review_artifacts": ["<copy exactly>"],
    "grid_review_artifacts": ["<copy exactly>"],
    "confidence": "direct",
    "reason": "matching release and first new blue deployment footprint"
  }}]
}}

Modify no other file. If images are unavailable, do not write output.
